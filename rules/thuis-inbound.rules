	
var String MspUpdate = ""
var Boolean inPackage = false

rule "MSP on update"
	when 
		Item Msp received update
	then
		if(Msp.state.toString.trim.notBlank){
	    		if(MspUpdate.toString.trim.startsWith("|||")){
	    			inPackage = true;
	    		}
	    		//only add data if it starte correct!
	    		if (inPackage){
	    			MspUpdate = MspUpdate + Msp.state.toString.trim()
	    			
	    		}
	    		
	    		
	    		if(inPackage && MspUpdate.endsWith(";;;")){
					val typeCmd = MspUpdate.substring(0,1);
					if (typeCmd.equals("r")) {
						println("Received remote command: " + MspUpdate)
						val channel = MspUpdate.substring(1,2)
						val state = MspUpdate.substring(2,3)
						
						if(channel.equals("D"))
						{
							if(state.equals("0")) callScript("off");
							if(state.equals("1")) callScript("movie");
							
						} 
						if(channel.equals("C"))
						{
							if(state.equals("0")) callScript("work");
							if(state.equals("1")) callScript("off");
							
						} 
					}
					//remote with AlarmLib
					else if(typeCmd.equals("a")){
						//big remote
						if (MspUpdate.startsWith("a1511")){
							callScript("movie");
						}
						else if (MspUpdate.startsWith("a1514")){
							callScript("off");
						}
						
						
						//tiny remote
						else if (MspUpdate.startsWith("a419D41")){
							callScript("movie");
						}
						else if (MspUpdate.startsWith("a419D42")){
							callScript("off");
						}
						else if (MspUpdate.startsWith("a419D44")){
							callScript("work");
						}
						else if (MspUpdate.startsWith("a419D48")){
							callScript("SleepTimer");
						}
						else{
							println("unknown remote for 'a' " +MspUpdate );
						}
					}
					else if (typeCmd.equals("c")){
						println("received Temperature: " + MspUpdate)
						var Number temp = 0.0f
						if(MspUpdate.substring(2,3).equals("3")){
							temp = Integer::parseInt(MspUpdate.substring(5,6)) * 10;
							temp = temp + Integer::parseInt(MspUpdate.substring(6,7));
							//the .value
							temp = temp + (Integer::parseInt(MspUpdate.substring(7,8)) * 0.1);
							postUpdate(Weather_Temp_Balcony, temp);	
						}
						else
						{
							println("unconfigured sensor...");
						}
						
					}
					else
					{
						println("unknown data: " + MspUpdate);
					}
					MspUpdate = "";
					inPackage = false;
				}				
		  }
end
